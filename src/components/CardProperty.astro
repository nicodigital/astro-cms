---
const { id, Title, Description, Thumb } = Astro.props;

---
<a href={`/property/${id}`} class="card bg-neutral-800 text-white px-2 py-3 flex flex-col gap-2 rounded-xl">
    <img class="rounded-xl opacity-30" src={`http://localhost:1337${Thumb}`} alt='' loading='lazy' decoding='async' />
    <h2 class="h2 font-bold" >
        {Title}
    </h2>
    <p>
        {Description.slice(0,150)}...
    </p>
    
</a>